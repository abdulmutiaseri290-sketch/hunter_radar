name: hunter_radar
description: "تطبيق رادار الصياد - نسخة اللاسلكي والمتجر"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  google_maps_flutter: ^2.5.0
  permission_handler: ^11.0.1
  record: ^5.0.4
  audioplayers: ^5.2.1

flutter:
  uses-material-design: true
